# Source files

Place your **PDF and other source documents** here. The application will read documents from this folder **only when you run a rebuild** (e.g. `python scripts/rebuild.py`).

- Supported: PDF (`.pdf`)
- The application does not auto-rebuild on startup; it uses existing data until you trigger a rebuild.
